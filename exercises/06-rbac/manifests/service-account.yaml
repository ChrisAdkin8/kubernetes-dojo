apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-reader-sa
  namespace: default
  labels:
    exercise: "06"
  # Annotation required for IRSA (IAM Roles for Service Accounts).
  # Uncomment and replace with the actual IAM role ARN after creating it.
  # annotations:
  #   eks.amazonaws.com/role-arn: "arn:aws:iam::ACCOUNT_ID:role/ROLE_NAME"
